import React from "react";
import { render } from "@testing-library/react";
import Sidebar from "../components/Sidebar";

test("should render a dropdown with all the kinds", () => {});

test("should show one kind pre selected in the dropdown", () => {});
